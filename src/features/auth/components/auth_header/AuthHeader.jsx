import styles from "./AuthHeader.module.css"

const AuthHeader = () => {
    return <header className={styles.header}>
        <div className={styles.item}> Mandem </div>
    </header>
}

export default AuthHeader